--公会系统

local SocAction = class("SocAction", cc.server.ActionBase)

--点击公会按钮，进入公会，获取的信息
function SocAction:flushAction( data )
	-- body
end

function SocAction:mobaiAction( data )
	-- body
end

function SocAction:armsAction( data )
	-- body
end

return SocAction
